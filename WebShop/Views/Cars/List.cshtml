    <h1>Все автомобили</h1>
<h3>@Model.currCategory</h3><!--Обращаемся к категории, которую хотим вывести-->
    <div class="row mt-5 mb-2">
    @{
        foreach ( Car car in Model.allCars)//выводим все автомобили
        {
            @Html.Partial("AllCars", car)
        }

    }
    </div>

